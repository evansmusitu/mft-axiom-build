name: MUSITU FMI Billing Topology Read-Only

on:
  push:
    branches: [fmi-revenue-certification-20260913]
    paths:
      - 'scripts/fmi_billing_topology_readonly.py'
      - '.github/workflows/fmi-billing-topology-readonly.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      CF_API: https://api.cloudflare.com/client/v4
      ACCOUNT_ID: 93f395f5121954671f92fffa453d6b61
      CLOUDFLARE_EMAIL: ${{ secrets.CLOUDFLARE_EMAIL }}
      CLOUDFLARE_API_KEY: ${{ secrets.CLOUDFLARE_GLOBAL_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - name: Require protected Cloudflare credentials
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_EMAIL"
          test -n "$CLOUDFLARE_API_KEY"
      - name: Inspect live FMI billing topology read-only
        shell: bash
        run: |
          set -euo pipefail
          python scripts/fmi_billing_topology_readonly.py
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: musitu-fmi-billing-topology-readonly
          path: |
            fmi-billing-topology-readonly.json
            fmi-billing-topology-readonly.sha256
          retention-days: 30
          if-no-files-found: error
