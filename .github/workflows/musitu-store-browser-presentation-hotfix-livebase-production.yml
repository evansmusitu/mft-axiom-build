name: MUSITU Store Browser Presentation Hotfix Live-Base Production — RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Retired one-time workflow
        run: |
          echo 'RETIRED: the one-time browser-presentation production transaction completed via recovery run 34581130498.'
          echo 'Do not reuse the old raw-multipart rollback harness.'
          exit 1
