const BUILD = 'MUSITU_FMI_APP_V1_20260915';
const PRODUCT = 'MUSITU Frontier Market Intelligence';

const securityHeaders = {
  'x-content-type-options': 'nosniff',
  'x-frame-options': 'DENY',
  'referrer-policy': 'no-referrer',
  'permissions-policy': 'camera=(), microphone=(), geolocation=(), payment=()',
  'cross-origin-opener-policy': 'same-origin',
  'cross-origin-resource-policy': 'same-origin',
  'content-security-policy': "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'self'; manifest-src 'self'; worker-src 'self'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'",
  'strict-transport-security': 'max-age=31536000; includeSubDomains',
};

function reply(body, status=200, contentType='text/plain; charset=utf-8', extra={}) {
  return new Response(body, { status, headers: { 'content-type': contentType, 'cache-control': 'no-store', ...securityHeaders, ...extra } });
}
function json(body, status=200, extra={}) { return reply(JSON.stringify(body), status, 'application/json; charset=utf-8', extra); }

const ICON = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512"><rect width="512" height="512" rx="112" fill="#080b12"/><path d="M104 344V168h58l94 106 94-106h58v176h-58V253l-94 102-94-102v91z" fill="#f2f5fb"/><circle cx="408" cy="110" r="28" fill="#7c9cff"/></svg>`;

const MANIFEST = JSON.stringify({
  id: '/app', name: 'MUSITU FMI', short_name: 'MUSITU FMI', description: 'MUSITU Frontier Market Intelligence customer workspace',
  start_url: '/app', scope: '/', display: 'standalone', background_color: '#080b12', theme_color: '#080b12', orientation: 'any',
  icons: [{ src: '/icon.svg', sizes: 'any', type: 'image/svg+xml', purpose: 'any maskable' }]
});

const HTML = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#080b12"><meta name="description" content="MUSITU Frontier Market Intelligence"><link rel="manifest" href="/manifest.webmanifest"><link rel="icon" href="/icon.svg"><link rel="apple-touch-icon" href="/icon.svg"><title>MUSITU FMI</title><link rel="stylesheet" href="/app.css"></head><body><div id="app"><div class="boot"><img src="/icon.svg" alt=""><strong>MUSITU FMI</strong><span>Opening intelligence workspace…</span></div></div><script src="/app.js" defer></script></body></html>`;

const CSS = `:root{color-scheme:dark;--bg:#070910;--panel:#0d111b;--panel2:#121827;--line:#232d40;--text:#f6f8fd;--muted:#9aa6ba;--accent:#86a4ff;--good:#58d69a;--warn:#f2c866;--bad:#ff7b87;--r:20px;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}html,body,#app{min-height:100%;margin:0}body{background:radial-gradient(circle at 20% -10%,#18223b 0,transparent 35%),var(--bg);color:var(--text)}button,input,select,textarea{font:inherit}.boot{min-height:100vh;display:grid;place-items:center;align-content:center;gap:12px;color:var(--muted)}.boot img{width:64px;height:64px}.boot strong{color:var(--text);font-size:22px}.auth{min-height:100vh;display:grid;place-items:center;padding:22px}.auth-card{width:min(540px,100%);border:1px solid var(--line);background:rgba(12,17,27,.96);border-radius:28px;padding:28px;box-shadow:0 30px 90px #0008}.eyebrow{font-size:11px;letter-spacing:.15em;text-transform:uppercase;color:var(--accent);font-weight:800}.auth-card h1{font-size:34px;line-height:1.05;margin:12px 0}.muted{color:var(--muted)}.tabs{display:flex;gap:8px;background:#080b12;border-radius:14px;padding:4px;margin:22px 0}.tabs button{flex:1}.field{display:grid;gap:7px;margin:14px 0}.field label{font-size:12px;color:var(--muted)}input,textarea,select{width:100%;border:1px solid var(--line);background:#080c14;color:var(--text);border-radius:13px;padding:12px 13px;outline:none}textarea{min-height:110px;resize:vertical}input:focus,textarea:focus,select:focus{border-color:var(--accent)}button{border:0;border-radius:13px;background:var(--accent);color:#07101f;padding:11px 14px;font-weight:800;cursor:pointer}button.secondary{background:#171f2d;color:var(--text);border:1px solid var(--line)}button.ghost{background:transparent;color:var(--muted)}button.danger{background:#33171c;color:#ffbcc3;border:1px solid #682c36}button:disabled{opacity:.5;cursor:not-allowed}.error{color:#ff9ca6;font-size:13px;margin-top:10px}.shell{display:grid;grid-template-columns:250px minmax(0,1fr);min-height:100vh}.side{border-right:1px solid var(--line);background:rgba(7,10,16,.94);padding:20px 14px;position:sticky;top:0;height:100vh}.brand{display:flex;gap:11px;align-items:center;padding:4px 8px 20px}.brand img{width:36px;height:36px;border-radius:10px}.brand strong{font-size:16px}.brand small{display:block;color:var(--muted);font-size:10px;margin-top:2px}.nav{display:grid;gap:5px}.nav button{text-align:left;background:transparent;color:var(--muted);padding:11px 12px}.nav button.on,.nav button:hover{background:#151c29;color:var(--text)}.side-foot{position:absolute;bottom:16px;left:14px;right:14px}.authority{font-size:11px;color:var(--warn);border:1px solid #65552a;background:#1a160b;padding:9px 10px;border-radius:12px;margin-bottom:8px}.main{padding:24px;max-width:1480px;width:100%;margin:0 auto}.top{display:flex;justify-content:space-between;align-items:flex-start;gap:15px;margin-bottom:20px}.top h1{margin:0;font-size:28px}.top p{margin:6px 0 0;color:var(--muted);font-size:13px}.top-actions{display:flex;gap:8px;flex-wrap:wrap}.grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:13px}.card{background:rgba(13,17,27,.95);border:1px solid var(--line);border-radius:var(--r);padding:17px}.metric .k{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}.metric .v{font-size:25px;font-weight:900;margin-top:8px}.metric .sub{color:var(--muted);font-size:12px;margin-top:5px}.section{margin-top:16px}.section h2{font-size:15px;margin:0 0 11px}.split{display:grid;grid-template-columns:1fr 1fr;gap:13px}.pill{display:inline-flex;border:1px solid var(--line);border-radius:999px;padding:4px 8px;font-size:10px;font-weight:800;letter-spacing:.04em}.pill.good{color:var(--good);border-color:#285d48}.pill.warn{color:var(--warn);border-color:#66552b}.pill.bad{color:var(--bad);border-color:#66323a}.toolbar{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-bottom:12px}.toolbar input{max-width:420px}.list{display:grid;gap:9px}.item{padding:12px;border:1px solid #1e2736;border-radius:14px;background:#0a0f18}.item-head{display:flex;justify-content:space-between;gap:10px}.item strong{font-size:13px}.item .meta{color:var(--muted);font-size:11px;margin-top:4px}.json{font:11px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap;word-break:break-word;color:#c9d3e4;background:#080c14;border:1px solid #1d2635;border-radius:13px;padding:12px;max-height:420px;overflow:auto}.kv{display:grid;grid-template-columns:180px minmax(0,1fr);gap:8px 14px;font-size:13px}.kv>div:nth-child(odd){color:var(--muted)}.statusbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:16px}.dot{width:8px;height:8px;border-radius:50%;background:var(--good)}.empty{padding:28px;text-align:center;color:var(--muted)}.chart{width:100%;height:180px;background:#090d15;border:1px solid #1b2431;border-radius:14px;overflow:hidden}.chart svg{width:100%;height:100%}.notice{padding:12px 14px;border-radius:14px;background:#11192a;border:1px solid #2a3852;color:#bdcae0;font-size:12px}.bottom{display:none}.toast{position:fixed;right:18px;bottom:18px;background:#171f2d;border:1px solid var(--line);border-radius:13px;padding:11px 14px;z-index:50;box-shadow:0 18px 50px #0008}.install{display:none}@media(max-width:1000px){.grid{grid-template-columns:repeat(2,1fr)}.split{grid-template-columns:1fr}}@media(max-width:760px){.shell{display:block;padding-bottom:74px}.side{display:none}.main{padding:16px 13px}.grid{grid-template-columns:1fr 1fr}.top{align-items:center}.top h1{font-size:24px}.bottom{position:fixed;display:grid;grid-template-columns:repeat(5,1fr);bottom:0;left:0;right:0;height:66px;background:rgba(8,11,18,.97);border-top:1px solid var(--line);z-index:40;padding-bottom:env(safe-area-inset-bottom)}.bottom button{background:transparent;color:var(--muted);font-size:10px;border-radius:0;padding:8px 2px}.bottom button.on{color:var(--accent)}.bottom span{display:block;font-size:18px;line-height:1.2}.install{display:inline-flex}.auth-card{padding:22px}.kv{grid-template-columns:1fr}.kv>div:nth-child(odd){margin-top:6px}}@media(max-width:480px){.grid{grid-template-columns:1fr}.top-actions .hide-small{display:none}}`;

const JS = `const BUILD='${BUILD}',app=document.getElementById('app');const S={view:'home',key:sessionStorage.getItem('fmi_key')||localStorage.getItem('fmi_key')||'',remember:!!localStorage.getItem('fmi_key'),me:null,install:null};
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const pretty=x=>JSON.stringify(x,null,2);const arr=x=>Array.isArray(x)?x:(Array.isArray(x?.items)?x.items:Array.isArray(x?.results)?x.results:Array.isArray(x?.data)?x.data:[]);
function saveKey(k,remember=false){S.key=k||'';S.remember=remember;sessionStorage.removeItem('fmi_key');localStorage.removeItem('fmi_key');if(k)(remember?localStorage:sessionStorage).setItem('fmi_key',k)}
async function api(path,opt={}){const h={'accept':'application/json',...(opt.headers||{})};if(S.key&&!opt.noAuth)h.authorization='Bearer '+S.key;if(opt.body&&typeof opt.body!=='string'){h['content-type']='application/json';opt.body=JSON.stringify(opt.body)}const r=await fetch('/api'+path,{...opt,headers:h,cache:'no-store'});let x=null;try{x=await r.json()}catch{x={error:'non_json_response'}}if(!r.ok){const e=new Error(x?.error||x?.message||('HTTP '+r.status));e.status=r.status;e.body=x;throw e}return x}
function pill(s){const t=String(s??'UNKNOWN');const c=/ready|active|ok|pass|paper_shadow_market_intelligence/i.test(t)?'good':/free|paper|shadow|pending|created/i.test(t)?'warn':'bad';return '<span class="pill '+c+'">'+esc(t)+'</span>'}
function toast(t){const d=document.createElement('div');d.className='toast';d.textContent=t;document.body.appendChild(d);setTimeout(()=>d.remove(),2600)}
function appHeader(title,sub){return '<div class="top"><div><h1>'+esc(title)+'</h1><p>'+esc(sub)+'</p></div><div class="top-actions"><button id="refresh" class="secondary">Refresh</button><button id="install" class="secondary install">Install</button></div></div>'}
const NAV=[['home','⌂','Home'],['markets','⌁','Markets'],['intel','✦','Intel'],['evidence','◎','Evidence'],['account','◉','Account'],['watchlists','☆','Watchlists'],['history','↺','History'],['billing','◇','Plans']];
function shell(){return '<div class="shell"><aside class="side"><div class="brand"><img src="/icon.svg"><div><strong>MUSITU FMI</strong><small>FRONTIER MARKET INTELLIGENCE</small></div></div><nav class="nav">'+NAV.map(n=>'<button data-view="'+n[0]+'" class="'+(S.view===n[0]?'on':'')+'">'+n[1]+' &nbsp;'+n[2]+'</button>').join('')+'</nav><div class="side-foot"><div class="authority">PAPER / SHADOW intelligence<br>No live trade execution</div><button id="signout" class="secondary" style="width:100%">Sign out</button></div></aside><main class="main" id="main"></main></div><nav class="bottom">'+NAV.slice(0,5).map(n=>'<button data-view="'+n[0]+'" class="'+(S.view===n[0]?'on':'')+'"><span>'+n[1]+'</span>'+n[2]+'</button>').join('')+'</nav>'}
function wireShell(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{S.view=b.dataset.view;renderShell()});const so=document.getElementById('signout');if(so)so.onclick=()=>{saveKey('');S.me=null;renderAuth()};const i=document.getElementById('install');if(i)i.onclick=async()=>{if(S.install){S.install.prompt();await S.install.userChoice;S.install=null}else toast('Use your browser menu → Install app / Add to Home screen')}}
async function renderShell(){app.innerHTML=shell();wireShell();await loadView()}
function renderAuth(){app.innerHTML='<div class="auth"><div class="auth-card"><div class="eyebrow">MUSITU · FMI</div><h1>Market intelligence, as an app.</h1><p class="muted">Create a free FMI account or connect an existing access key. Your key stays on this device and is only sent to the MUSITU FMI origin.</p><div class="tabs"><button id="newTab">Create free account</button><button id="keyTab" class="secondary">Use access key</button></div><div id="authBody"></div><p class="muted" style="font-size:11px;margin-top:18px">Current authority: PAPER_SHADOW_ONLY · Live trade execution is not enabled.</p></div></div>';showSignup();document.getElementById('newTab').onclick=showSignup;document.getElementById('keyTab').onclick=showKey}
function showSignup(){const b=document.getElementById('authBody');b.innerHTML='<form id="signup"><div class="field"><label>Email</label><input id="email" type="email" autocomplete="email" required></div><div class="field"><label>Name</label><input id="name" autocomplete="name" placeholder="Optional"></div><label style="display:flex;gap:8px;align-items:center;font-size:12px;color:var(--muted);margin:12px 0"><input id="remember" type="checkbox" style="width:auto"> Remember this device</label><button type="submit" style="width:100%">Create free workspace</button><div id="authErr" class="error"></div></form>';document.getElementById('signup').onsubmit=async e=>{e.preventDefault();const er=document.getElementById('authErr');er.textContent='';try{const x=await api('/v1/signup',{method:'POST',noAuth:true,body:{email:document.getElementById('email').value.trim(),name:document.getElementById('name').value.trim(),plan:'FREE'}});const k=x.api_key||x.key||x.token;if(!k)throw new Error('FMI did not return an access key');saveKey(k,document.getElementById('remember').checked);await validate()}catch(x){er.textContent=x.message}}}
function showKey(){const b=document.getElementById('authBody');b.innerHTML='<form id="connect"><div class="field"><label>FMI access key</label><input id="key" type="password" autocomplete="off" required></div><label style="display:flex;gap:8px;align-items:center;font-size:12px;color:var(--muted);margin:12px 0"><input id="remember" type="checkbox" style="width:auto"> Remember this device</label><button type="submit" style="width:100%">Open workspace</button><div id="authErr" class="error"></div></form>';document.getElementById('connect').onsubmit=async e=>{e.preventDefault();const er=document.getElementById('authErr');er.textContent='';const old=S.key;try{saveKey(document.getElementById('key').value.trim(),document.getElementById('remember').checked);await validate()}catch(x){saveKey(old,S.remember);er.textContent=x.message}}}
async function validate(){S.me=await api('/v1/me');await renderShell()}
async function loadView(){const m=document.getElementById('main');m.innerHTML='<div class="boot" style="min-height:60vh"><strong>Loading</strong></div>';try{if(S.view==='home')await home(m);if(S.view==='markets')await markets(m);if(S.view==='intel')await intel(m);if(S.view==='evidence')await evidence(m);if(S.view==='watchlists')await watchlists(m);if(S.view==='history')await history(m);if(S.view==='billing')await billing(m);if(S.view==='account')await account(m);const r=document.getElementById('refresh');if(r)r.onclick=loadView}catch(e){if(e.status===401){saveKey('');S.me=null;return renderAuth()}m.innerHTML=appHeader('Unable to load','The live FMI API returned an error.')+'<div class="card"><div class="error">'+esc(e.message)+'</div><pre class="json">'+esc(pretty(e.body||{}))+'</pre></div>';const r=document.getElementById('refresh');if(r)r.onclick=loadView}}
async function home(m){const [me,h,w]=await Promise.all([S.me||api('/v1/me'),api('/v1/history'),api('/v1/watchlists')]);S.me=me;const hs=arr(h),ws=arr(w);m.innerHTML=appHeader('Intelligence workspace','Real FMI account state · '+new Date().toLocaleString())+'<div class="statusbar"><span class="dot"></span><span>Connected to FMI</span>'+pill(me.plan||me.customer?.plan||'ACTIVE')+pill('PAPER / SHADOW')+'</div><div class="grid">'+[['Plan',me.plan||me.customer?.plan||'—','Provider-verified entitlements'],['Research history',hs.length,'Stored intelligence records'],['Watchlists',ws.length,'Customer-owned collections'],['Trade execution','OFF','Intelligence only']].map(x=>'<div class="card metric"><div class="k">'+x[0]+'</div><div class="v">'+esc(x[1])+'</div><div class="sub">'+x[2]+'</div></div>').join('')+'</div><div class="split section"><div class="card"><h2>Account</h2>'+kv(me)+'</div><div class="card"><h2>Start research</h2><p class="muted">Search a market, inspect live FMI context, then run evidence-backed paper/shadow intelligence.</p><button id="goMarket">Open Markets</button></div></div>';document.getElementById('goMarket').onclick=()=>{S.view='markets';renderShell()}}
function kv(o){if(!o||typeof o!=='object')return '<pre class="json">'+esc(pretty(o))+'</pre>';const entries=Object.entries(o).filter(([,v])=>['string','number','boolean'].includes(typeof v)).slice(0,16);return entries.length?'<div class="kv">'+entries.map(([k,v])=>'<div>'+esc(k.replaceAll('_',' '))+'</div><div>'+esc(v)+'</div>').join('')+'</div>':'<pre class="json">'+esc(pretty(o))+'</pre>'}
function cards(x){const a=arr(x);if(!a.length)return '<pre class="json">'+esc(pretty(x))+'</pre>';return '<div class="list">'+a.slice(0,60).map((v,i)=>'<div class="item"><div class="item-head"><strong>'+esc(v.symbol||v.name||v.title||v.id||('Record '+(i+1)))+'</strong>'+(v.status?pill(v.status):'')+'</div><div class="meta">'+esc(v.horizon||v.created_at||v.updated_at||v.occurred_at||'')+'</div><pre class="json">'+esc(pretty(v))+'</pre></div>').join('')+'</div>'}
async function markets(m){m.innerHTML=appHeader('Markets','Search the live FMI market surface. No fabricated quotes.')+'<div class="toolbar"><input id="mq" placeholder="EURUSD, XAUUSD, NAS100…"><button id="ms">Search</button><button id="mc" class="secondary">Load context</button></div><div id="marketOut" class="card"><div class="empty">Enter a symbol or market query.</div></div>';const out=document.getElementById('marketOut'),q=()=>document.getElementById('mq').value.trim();document.getElementById('ms').onclick=async()=>{try{const x=await api('/v1/market/search?q='+encodeURIComponent(q())+'&query='+encodeURIComponent(q()));out.innerHTML='<h2>Search results</h2>'+cards(x)}catch(e){out.innerHTML='<div class="error">'+esc(e.message)+'</div><pre class="json">'+esc(pretty(e.body||{}))+'</pre>'}};document.getElementById('mc').onclick=async()=>{try{const x=await api('/v1/market/context?symbol='+encodeURIComponent(q()));out.innerHTML='<h2>Market context</h2>'+cards(x)}catch(e){out.innerHTML='<div class="error">'+esc(e.message)+'</div><pre class="json">'+esc(pretty(e.body||{}))+'</pre>'}}}
async function intel(m){m.innerHTML=appHeader('Intelligence','Run the live FMI analysis engine with observed inputs.')+'<div class="split"><div class="card"><form id="an"><div class="field"><label>Symbol</label><input id="sym" value="EURUSD" required></div><div class="field"><label>Horizon</label><select id="hor"><option>NEXT_4H</option><option>NEXT</option><option>NEXT_1H</option><option>NEXT_1D</option></select></div><div class="field"><label>Observed returns</label><textarea id="ret" placeholder="0.001, -0.0004, 0.0008, 0.0012, -0.0003" required></textarea></div><div class="field"><label>Imbalance</label><input id="imb" type="number" step="any" value="0"></div><button type="submit">Analyze with FMI</button><div id="anErr" class="error"></div></form></div><div class="card"><h2>Analysis result</h2><div id="anOut" class="empty">Your live FMI result will appear here.</div></div></div><div class="section notice">MUSITU FMI does not invent return series. Use observed market data. Results remain PAPER_SHADOW_MARKET_INTELLIGENCE until higher authority is separately certified.</div>';document.getElementById('an').onsubmit=async e=>{e.preventDefault();const er=document.getElementById('anErr'),out=document.getElementById('anOut');er.textContent='';try{const returns=document.getElementById('ret').value.split(/[\s,]+/).filter(Boolean).map(Number);if(returns.length<2||returns.some(x=>!Number.isFinite(x)))throw new Error('Enter at least two numeric returns');const x=await api('/v1/intelligence/analyze',{method:'POST',body:{symbol:document.getElementById('sym').value.trim().toUpperCase(),horizon:document.getElementById('hor').value,returns,imbalance:Number(document.getElementById('imb').value||0),source_ids:['musitu-fmi-app-user-observed']}});out.innerHTML=(x.classification?pill(x.classification):'')+'<pre class="json">'+esc(pretty(x))+'</pre>'}catch(x){er.textContent=x.message;out.innerHTML='<pre class="json">'+esc(pretty(x.body||{}))+'</pre>'}}}
async function evidence(m){const events=await api('/v1/evidence/monitor-events?limit=50');m.innerHTML=appHeader('Evidence monitors','In-app monitoring on new evidence; no hidden autonomous trading.')+'<div class="split"><div class="card"><h2>Create monitor</h2><form id="mon"><div class="field"><label>Name</label><input id="mn" value="Market evidence watch" required></div><div class="field"><label>Symbol</label><input id="msym" value="EURUSD" required></div><div class="field"><label>Horizon</label><input id="mh" value="NEXT_4H" required></div><div class="field"><label>Volatility absolute delta ≥</label><input id="mv" type="number" step="any" value="0.0002"></div><label style="display:flex;gap:8px;align-items:center;font-size:12px;color:var(--muted);margin:10px 0"><input id="mr" type="checkbox" style="width:auto" checked> Alert on regime change</label><button>Create monitor</button><div id="monErr" class="error"></div></form></div><div class="card"><h2>Recent monitor events</h2><div id="events">'+cards(events)+'</div></div></div>';document.getElementById('mon').onsubmit=async e=>{e.preventDefault();const er=document.getElementById('monErr');er.textContent='';try{await api('/v1/evidence/monitors',{method:'POST',body:{name:document.getElementById('mn').value.trim(),symbol:document.getElementById('msym').value.trim().toUpperCase(),horizon:document.getElementById('mh').value.trim(),match_mode:'ANY',conditions:{regime_change:document.getElementById('mr').checked,mean_return_abs_delta_gte:null,volatility_abs_delta_gte:Number(document.getElementById('mv').value||0),trust_deteriorated:false,provider_change:false,source_mode_change:false}}});toast('Monitor created');await evidence(m)}catch(x){er.textContent=x.message}}}
async function watchlists(m){const x=await api('/v1/watchlists');m.innerHTML=appHeader('Watchlists','Live customer-owned watchlist data.')+'<div class="card">'+cards(x)+'</div>'}
async function history(m){const x=await api('/v1/history');m.innerHTML=appHeader('History','Your stored FMI intelligence activity.')+'<div class="card">'+cards(x)+'</div>'}
async function billing(m){const x=await api('/v1/billing/catalog',{noAuth:true});m.innerHTML=appHeader('Plans','Provider-verified entitlements only.')+'<div class="card"><div class="notice">MUSITU FMI never grants a paid plan locally. Payment must be verified by the provider before entitlement changes.</div><div class="section">'+cards(x)+'</div></div><div class="section card"><h2>Upgrade</h2><p class="muted">Checkout is only initiated after your explicit confirmation.</p><div class="toolbar"><select id="plan"><option>FOUNDING</option><option>PRO</option></select><button id="checkout">Start checkout</button></div><div id="checkoutOut"></div></div>';document.getElementById('checkout').onclick=async()=>{const p=document.getElementById('plan').value;if(!confirm('Start a real '+p+' checkout with the configured payment provider?'))return;const out=document.getElementById('checkoutOut');try{const y=await api('/v1/billing/checkout',{method:'POST',body:{plan:p}});out.innerHTML='<pre class="json">'+esc(pretty(y))+'</pre>';const u=y.redirect_url||y.checkout_url||y.url;if(u&&confirm('Open the payment provider now?'))location.href=u}catch(e){out.innerHTML='<div class="error">'+esc(e.message)+'</div><pre class="json">'+esc(pretty(e.body||{}))+'</pre>'}}}
async function account(m){const me=S.me||await api('/v1/me');m.innerHTML=appHeader('Account','Device access and authority boundaries.')+'<div class="split"><div class="card"><h2>FMI account</h2>'+kv(me)+'</div><div class="card"><h2>This device</h2><div class="kv"><div>Credential storage</div><div>'+(S.remember?'Remembered on device':'Session only')+'</div><div>Build</div><div>'+BUILD+'</div><div>Authority</div><div>PAPER_SHADOW_ONLY</div><div>Live trading</div><div>Not authorized</div></div><div class="toolbar section"><button id="forget" class="danger">Forget access key</button></div></div></div>';document.getElementById('forget').onclick=()=>{if(confirm('Forget the FMI access key on this device?')){saveKey('');S.me=null;renderAuth()}}}
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();S.install=e});if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});(async()=>{if(S.key){try{await validate()}catch{saveKey('');renderAuth()}}else renderAuth()})();`;

const SW = `const C='musitu-fmi-app-v1';const A=['/app','/app.css','/app.js','/manifest.webmanifest','/icon.svg'];self.addEventListener('install',e=>e.waitUntil(caches.open(C).then(c=>c.addAll(A)).then(()=>self.skipWaiting())));self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(k=>Promise.all(k.filter(x=>x!==C).map(x=>caches.delete(x)))).then(()=>self.clients.claim())));self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==location.origin||u.pathname.startsWith('/api/'))return;if(e.request.method!=='GET')return;e.respondWith(fetch(e.request).then(r=>{const c=r.clone();caches.open(C).then(x=>x.put(e.request,c));return r}).catch(()=>caches.match(e.request).then(r=>r||caches.match('/app'))))});`;

async function proxy(request, env, url) {
  const upstream = String(env.PUBLIC_BASE || '').replace(/\/$/, '');
  if (!upstream) return json({ ok:false, error:'upstream_not_configured' }, 503);
  const target = new URL(upstream + url.pathname.slice(4) + url.search);
  const headers = new Headers(request.headers);
  headers.delete('host'); headers.delete('cf-connecting-ip'); headers.delete('cf-ipcountry'); headers.delete('cf-ray'); headers.delete('x-forwarded-for');
  headers.set('accept', headers.get('accept') || 'application/json');
  headers.set('user-agent', 'MUSITU-FMI-App/1.0');
  const init = { method: request.method, headers, redirect: 'manual' };
  if (!['GET','HEAD'].includes(request.method)) init.body = await request.arrayBuffer();
  let r;
  try { r = await fetch(target.toString(), init); }
  catch { return json({ ok:false, error:'upstream_unavailable' }, 502); }
  const rh = new Headers(r.headers); rh.delete('set-cookie');
  for (const [k,v] of Object.entries(securityHeaders)) rh.set(k,v);
  rh.set('cache-control','no-store'); rh.set('x-musitu-fmi-app-build', BUILD);
  return new Response(r.body, { status:r.status, statusText:r.statusText, headers:rh });
}

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (url.pathname === '/healthz') {
      let upstream = { ok:false, status:null };
      try { const r=await fetch(String(env.PUBLIC_BASE).replace(/\/$/,'')+'/healthz',{headers:{accept:'application/json','user-agent':'MUSITU-FMI-App/1.0'}}); upstream.status=r.status; upstream.ok=r.ok; if((r.headers.get('content-type')||'').includes('json'))upstream.body=await r.json(); } catch { upstream.error='fetch_error'; }
      return json({ ok:upstream.ok, product:PRODUCT, surface:'customer_app', build:BUILD, installable:true, upstream, authority:'PAPER_SHADOW_ONLY', live_trading_authorized:false }, upstream.ok?200:503);
    }
    if (url.pathname.startsWith('/api/')) return proxy(request, env, url);
    if (request.method !== 'GET') return json({ok:false,error:'method_not_allowed'},405);
    if (url.pathname === '/' || url.pathname === '/app' || url.pathname === '/app/') return reply(HTML,200,'text/html; charset=utf-8');
    if (url.pathname === '/app.css') return reply(CSS,200,'text/css; charset=utf-8');
    if (url.pathname === '/app.js') return reply(JS,200,'application/javascript; charset=utf-8');
    if (url.pathname === '/sw.js') return reply(SW,200,'application/javascript; charset=utf-8',{'service-worker-allowed':'/'});
    if (url.pathname === '/manifest.webmanifest') return reply(MANIFEST,200,'application/manifest+json; charset=utf-8');
    if (url.pathname === '/icon.svg') return reply(ICON,200,'image/svg+xml; charset=utf-8');
    return reply('Not found',404);
  }
};
