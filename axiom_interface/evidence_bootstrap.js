import {initEvidenceObservatory} from './evidence_ui.js';
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
function installStyles(){if(document.querySelector('link[data-axiom-evidence-style]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='./styles/evidence.css';link.dataset.axiomEvidenceStyle='true';document.head.append(link);}
async function dependency(){for(let attempt=0;attempt<400;attempt++){if(window.AxiomDeveloperPlatform?.store)return window.AxiomDeveloperPlatform;await sleep(25);}throw new Error('Phase 12 developer platform candidate API was not ready');}
async function boot(){installStyles();const developer=await dependency(),api=await initEvidenceObservatory({emit:window.AxiomUI?.emit,developer});window.AxiomUI?.emit?.('workspace.phase13.candidate-ready',{state:'awaiting-authenticated-independent-review'});return api;}
window.AxiomEvidenceObservatoryBootstrap=boot().catch(error=>{window.AxiomUI?.reportError?.({errorId:'AXIOM-EVIDENCE-INIT',component:'Evidence Observatory',impact:'Evidence and trust preview unavailable; inherited workspaces remain available',failed:error.message,recovery:'Reload and retry the Evidence surface'});throw error;});
queueMicrotask(()=>{void window.AxiomEvidenceObservatoryBootstrap.then(()=>import('./pwa_bootstrap.js')).catch(error=>window.AxiomUI?.reportError?.({errorId:'AXIOM-PWA-MODULE',component:'PWA and mobile resilience',impact:'PWA resilience controls unavailable; inherited workspaces remain available',failed:error.message,recovery:'Reload the workspace and retry Settings'}));});
