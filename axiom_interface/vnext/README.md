# MUSITU AXIOM vNext application

This directory is an isolated implementation of the audit-grounded final application redesign.

## Authority boundary

- Base: sealed browser-production lineage at `c13bee58c350c82bb47ba58136458240f28f13ee`.
- Existing `axiom_interface/` production application remains unchanged and is the rollback origin.
- vNext is **not** production promotion authority.
- Phase 13 remains skipped/unearned.
- Phase 14 remains incomplete until its required real-device matrix is actually satisfied.
- Phase 15 and global-superiority claims remain blocked until external evidence exists.

## Current foundation implemented

- outcome-first Home;
- global application shell;
- contextual Inspector instead of an always-branded Proof Drawer;
- Projects, Work, Agents, Artifacts, Research, Analyze, Build, Create, Live, Computer, Automations, Developer, Trust and Settings surfaces;
- universal composer;
- command palette;
- secure reuse of the existing same-origin browser-session adapter;
- deterministic local capability router backed by the certified 74-operation registry;
- generated native-catalog cardinality of 2,400 compositions (24 archetypes × 10 intents × 10 contexts);
- explicit "preview only" boundary for local routing — no consequential external action is executed by this scaffold.

## Next engineering steps

Connect the shell to the earned project/work/research/artifact/observability/agent/memory/developer stores through typed adapters, add the Build Studio execution substrate, then run the sealed AXIOM-builds-AXIOM acceptance challenge before any production cutover.
